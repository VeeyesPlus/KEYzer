import sys; sys.setrecursionlimit(sys.getrecursionlimit() * 5)

# kwgen_gui.spec

block_cipher = None

a = Analysis(['kwgen_gui.py'],
             pathex=['C:/Users/vpadimiti/AppData/Local/Programs/Python/Python39/myenv'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)

pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)

exe = EXE(pyz,
          a.scripts,
          a.binaries,
          a.zipfiles,
          a.datas,
          [],
          name='kwgen_gui',
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          upx_exclude=[],
          runtime_tmpdir=None,
          console=True )
